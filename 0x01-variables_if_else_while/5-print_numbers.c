#include <stdio.h>
/**
  *main - Entry point
  *
  *Return: Always 0 (Success)
  */
int main(void)
{
	int contador;

	printf("\n");

	contador = 0;

	while (contador <= 9)

	{
		printf("%d ", contador);
		contador++;
	}
	return (0);
}
